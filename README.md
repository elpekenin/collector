Small program to manage collecting Pokemon (not trainers nor energies) cards

Usage:
1. Compile the program with `zig build`. You'll find the binary under `zig-out/bin`. You can move/copy it wherever you want
1. Run it to start an interactive shell (REPL) to manage your collection. Use `help` to see available operations
