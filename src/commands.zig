pub const db = @import("commands/db.zig").run;
pub const download = @import("commands/download.zig").run;
pub const exit = @import("commands/exit.zig").run;
pub const help = @import("commands/help.zig").run;
pub const list = @import("commands/list.zig").run;
pub const row = @import("commands/row.zig").run;
